---
description: "Manthan Module 6 — Build a polished prototype (visual scaffold + reference patterns + per-screen spec + iteration). ~37 min."
---

**START TEACHING IMMEDIATELY. Do NOT summarize, narrate, or explain what you're about to do.**

Read silently in this order:

1. `.claude/SCRIPT_INSTRUCTIONS.md` — teaching rules (no fourth wall).
2. `PRINCIPLES.md` and `DOCUMENT-STANDARDS.md` — gates for every artifact.
3. `lesson-modules/m1-idea-to-prototype/m1.6-build-prototype/CLAUDE.md` — the teaching script.
4. Confirm prior outputs exist (`outputs/m1.1` through `m1.5`).
5. Confirm `reference/visual-scaffold/scaffold.css` and `reference/visual-scaffold/reference-patterns.md` exist.

Then jump to the script's `## Teaching Flow` section and begin with the first **SAY:** block.

When the artifact set is ready, call `principle-auditor` against the FULL artifact set (M1 through M6), display verdict, then close with the v1 PRD framing.
