---
description: "Manthan Module 6 — Build a polished prototype (visual scaffold + reference patterns + per-screen spec + iteration)."
---

**START TEACHING IMMEDIATELY. Do NOT narrate.**

Read `@lesson-modules/m1-idea-to-prototype/m1.6-build-prototype/CLAUDE.md` silently, then begin with the first **SAY:** block under `## Teaching Flow`.

When the artifact set is ready, call `principle-auditor` against the FULL artifact set (M1 through M6), display verdict, then close with the v1 PRD framing.
